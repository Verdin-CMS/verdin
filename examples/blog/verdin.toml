# Example project. The database comes from VERDIN_DATABASE_URL (or .env next to this
# file), e.g. VERDIN_DATABASE_URL=sqlite://data/blog.db — relative SQLite paths are
# resolved against this directory.

[server]
port = 1337
