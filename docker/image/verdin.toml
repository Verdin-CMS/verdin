# Configuration baked into the Docker image. Override any key with
# VERDIN_<SECTION>__<KEY>, or mount your own file and set VERDIN_CONFIG.

[server]
host = "0.0.0.0"
port = 1337

[schema]
path = "/data/schema"

[log]
format = "json"

[upload]
provider = { name = "local", dir = "/data/uploads" }
